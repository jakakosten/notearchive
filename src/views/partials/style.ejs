<style>
  @keyframes flash {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0;
    }
  }

  body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f7f7f7;
  }

  header {
    background-color: #4fb7ea;
    color: #fff;
    padding: 10px;
    text-align: center;
  }

  header > div > * {
    margin-block: auto;
  }

  h1 {
    margin: 20px 0;
    text-align: center;
  }

  header p {
    color: black !important;
  }

  header a,
  header p,
  header a::after {
    color: white !important;
    text-decoration: none !important;
    transition: all 100ms ease-in-out !important;
  }

  .container {
    max-width: 1350px;
    margin: 0 auto;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
  }

  .header-container {
    display: flex !important;
    justify-content: space-between !important;
    align-items: center !important;
  }

  .dropdown-item {
    color: #333 !important;
  }

  .header-container a:hover {
    color: rgb(240, 240, 240);
  }

  .dropdown.text-end > a {
    display: flex !important;
    gap: 1em;
  }

  .dropdown.text-end > a::after {
    margin-block: auto;
  }

  .dropdown.text-end > a > * {
    margin-block: auto !important;
  }

  .search-container {
    display: flex;
    gap: 0.5em;
  }

  .entry-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
  }

  .entry-table th,
  .entry-table td {
    text-align: center !important;
    vertical-align: middle !important;
  }

  .entry-table th,
  .entry-table td {
    border: 1px solid #ddd;
    padding: 8px;
  }

  .entry-row:hover {
    background-color: #f2f2f2;
  }

  .no-entries {
    color: red;
    animation: flash 1s infinite;
    text-align: center;
    font-weight: bold;
    margin-top: 10px;
    border: 1px solid red; /* Add border */
    padding: 10px; /* Add padding */
  }

  .entry-form {
    border: 1px solid #ddd;
    padding: 20px;
    background-color: #f7f7f7;
    margin-top: 20px;
    border-radius: 5px;
  }

  .entry-form label {
    display: block;
    margin-top: 10px;
    font-weight: bold;
  }

  .entry-form input[type="text"],
  .entry-form textarea {
    width: 100%;
    padding: 8px;
    border: 1px solid #ddd;
    border-radius: 3px;
  }

  .entry-form input[type="submit"] {
    margin-top: 10px;
    background-color: #333;
    color: #fff;
    border: none;
    padding: 8px 15px;
    border-radius: 3px;
    cursor: pointer;
  }

  .entry-form input[type="submit"]:hover {
    background-color: #555;
  }

  .entry-form {
    border: 1px solid #ddd;
    padding: 20px;
    background-color: #f7f7f7;
    margin-top: 20px;
    border-radius: 5px;
  }

  .entry-form label {
    display: block;
    margin-top: 10px;
    font-weight: bold;
  }

  .entry-form input[type="text"],
  .entry-form textarea {
    width: 100%;
    padding: 8px;
    border: 1px solid #ddd;
    border-radius: 3px;
  }

  .entry-form input[type="submit"] {
    margin-top: 10px;
    background-color: #333;
    color: #fff;
    border: none;
    padding: 8px 15px;
    border-radius: 3px;
    cursor: pointer;
  }

  .entry-form input[type="submit"]:hover {
    background-color: #555;
  }

  .entry-form {
    max-height: 0;
    opacity: 0;
    visibility: hidden;
    overflow: hidden;
    transition: max-height 0s linear 0.5s, opacity 0.5s ease-in-out,
      visibility 0s linear 0.5s;
  }

  .entry-form.show {
    max-height: 1000px; /* Adjust the value as needed */
    opacity: 1;
    visibility: visible;
    transition: max-height 0.5s ease-in-out, opacity 0.5s ease-in-out,
      visibility 0s linear 0s;
  }

  .modal-background {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: 1000;
  }

  /* Style for the modal content */
  .modal-content {
    background-color: #fff;
    max-width: 400px;
    margin: 100px auto;
    padding: 20px;
    border-radius: 5px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
  }

  .table-top {
    display: flex;
    justify-content: space-between;
    padding-block: 1em;
  }

  .table-top > :nth-child(1) {
    width: 50%;
  }

  .table-top > * {
    margin-block: auto;
  }

  .form-floating.search-container.search-box {
    width: 65%;
  }

  .form-floating.search-filters {
    width: 20%;
  }

  .form-floating {
    width: 30%;
  }
</style>
